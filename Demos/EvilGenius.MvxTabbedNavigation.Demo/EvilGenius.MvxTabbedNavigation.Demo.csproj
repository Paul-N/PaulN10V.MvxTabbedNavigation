<Project Sdk="Microsoft.NET.Sdk">

	<PropertyGroup>
		<TargetFrameworks>net10.0-android;net10.0-ios;net10.0-maccatalyst</TargetFrameworks>
		<OutputType>Exe</OutputType>
		<RootNamespace>EvilGenius.MvxTabbedNavigation.Demo</RootNamespace>
		<SingleProject>true</SingleProject>
		<ImplicitUsings>enable</ImplicitUsings>
		<UseMaui>true</UseMaui>
		<AndroidLinkMode>None</AndroidLinkMode>
<!--		<UseMauiEssentials>true</UseMauiEssentials>-->
		<GenerateAssemblyInfo>true</GenerateAssemblyInfo>
		<!-- Display name -->
		<ApplicationTitle>TabbedNavigation Demo</ApplicationTitle>

		<!-- App Identifier -->
		<ApplicationId>com.evilgenius.tabbednavigationdemo</ApplicationId>

		<!-- Versions -->
		<ApplicationDisplayVersion>1.0</ApplicationDisplayVersion>
		<ApplicationVersion>1</ApplicationVersion>
		<!-- Ensure referenced assemblies are copied to the output for Android builds -->

<!--		<SupportedOSPlatformVersion Condition="$([MSBuild]::GetTargetPlatformIdentifier('$(TargetFramework)')) == 'ios'">14.2</SupportedOSPlatformVersion>-->
		<SupportedOSPlatformVersion Condition="$([MSBuild]::GetTargetPlatformIdentifier('$(TargetFramework)')) == 'maccatalyst'">14.0</SupportedOSPlatformVersion>
<!--		<SupportedOSPlatformVersion Condition="$([MSBuild]::GetTargetPlatformIdentifier('$(TargetFramework)')) == 'android'">23.0</SupportedOSPlatformVersion>-->

		<SupportedOSPlatformVersion Condition="$([MSBuild]::GetTargetPlatformIdentifier('$(TargetFramework)')) == 'ios'">14.2</SupportedOSPlatformVersion>
		<SupportedOSPlatformVersion Condition="$([MSBuild]::GetTargetPlatformIdentifier('$(TargetFramework)')) == 'android'">23.0</SupportedOSPlatformVersion>
		<AndroidSdkCurrentVersion Condition="$([MSBuild]::GetTargetPlatformIdentifier('$(TargetFramework)')) == 'android'">36.0</AndroidSdkCurrentVersion>
		<AndroidMinSdkVersion Condition="$([MSBuild]::GetTargetPlatformIdentifier('$(TargetFramework)')) == 'android'">23.0</AndroidMinSdkVersion>

		<TargetPlatformVersion Condition="$([MSBuild]::GetTargetPlatformIdentifier('$(TargetFramework)')) == 'ios'">26.5</TargetPlatformVersion>
		<TargetPlatformVersion Condition="$([MSBuild]::GetTargetPlatformIdentifier('$(TargetFramework)')) == 'android'">36.0</TargetPlatformVersion>
	</PropertyGroup>

	<PropertyGroup>
	  <DefineConstants>$(DefineConstants)TRACE;SINGLE_PRJ</DefineConstants>
	</PropertyGroup>

	<ItemGroup>
		<!-- App Icon -->
		<MauiIcon Include="Resources\AppIcon\appicon.svg" ForegroundFile="Resources\AppIcon\appiconfg.svg" Color="#512BD4" />
		<MauiIcon Include="Resources\Images\dotnet_bot.svg" BaseSize="168,208" />

		<!-- Splash Screen -->
		<MauiSplashScreen Include="Resources\Splash\splash.svg" Color="#512BD4" BaseSize="128,128" />

		<!-- Images -->
		<MauiImage Include="Resources\Images\*" />

		<!-- Custom Fonts -->

		<!-- Raw Assets (also remove the "Resources\Raw" prefix) -->
		<MauiAsset Include="Resources\Raw\**" LogicalName="%(RecursiveDir)%(Filename)%(Extension)" />
	</ItemGroup>

	<ItemGroup>
	  <MauiImage Remove="Resources\Images\dotnet_bot.svg" />
	</ItemGroup>

	<ItemGroup>
<!--		<PackageReference Include="Microsoft.Maui.Controls" Version="10.0.20" />-->
		<PackageReference Include="MvvmCross" Version="10.1.2" />
		<PackageReference Include="MvvmCross.Plugin.Color" Version="10.1.2" />
		<PackageReference Include="Serilog.Extensions.Logging" Version="8.0.0" />
		<PackageReference Include="Serilog.Sinks.Debug" Version="3.0.0" />
	</ItemGroup>

	<ItemGroup>
	  <ProjectReference Include="..\..\EvilGenius.MvxTabbedNavigation\EvilGenius.MvxTabbedNavigation.csproj" />
	  <ProjectReference Include="..\EvilGenius.MvxTabbedNavigation.Demo.Core\EvilGenius.MvxTabbedNavigation.Demo.Core.csproj" />
	</ItemGroup>

	<!-- Copy the resolved ProjectReference output for the library into the Demo's intermediate
		 linker folders so Java.Interop can resolve platform-specific interface types. This uses
		 the resolved ReferenceCopyLocalPaths item (populated by ResolveAssemblyReferences) so
		 the correct per-TFM runtime assembly is used rather than a hard-coded hint path. -->
	<Target Name="CopyProjectReferenceOutputsToLinker" BeforeTargets="_LinkAssembliesNoShrink;LinkAssembliesNoShrink" Condition="'$(TargetFramework)' == 'net10.0-android'">
	  <ItemGroup>
		<!-- pick the resolved copy-local references that match the library assembly filename -->
		<_LibraryResolvedAssembly Include="@(ReferenceCopyLocalPaths)" Condition=" '%(ReferenceCopyLocalPaths.Filename)' == 'EvilGenius.MvxTabbedNavigation' " />
	  </ItemGroup>

	  <Message Importance="high" Text="Copying resolved project reference assemblies: %(_LibraryResolvedAssembly.Identity) -> $(IntermediateOutputPath)lp\999\jl\" />

	  <MakeDir Directories="$(IntermediateOutputPath)lp\999\jl\" />
	  <Copy SourceFiles="@(_LibraryResolvedAssembly)" DestinationFolder="$(IntermediateOutputPath)lp\999\jl\" SkipUnchangedFiles="true" />
	  <Copy SourceFiles="@(_LibraryResolvedAssembly)" DestinationFolder="$(IntermediateOutputPath)" SkipUnchangedFiles="true" />
	</Target>
</Project>
